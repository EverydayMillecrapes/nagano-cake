<%= render 'genrebox', genres: @genres %>
          <% if params[:genre_id] %>
            <%= @genre.name %>
          <% else %>
            すべての商品
          <% end %>
     
          全
          <% if params[:genre_id] %>
    	     <%= @products.order(created_at: :desc).where(sale_status: "販売可").count %>
          <% else %>
    		    <%= Product.where(sale_status: "販売可").count %>
          <% end %>
          件
        
	    	<% @products.each do |product| %>
         
		        <%= link_to customer_product_path(product) do %>
		          <%= attachment_image_tag product, :image, size: "150x150", format: 'jpeg', fallback: "noimage.jpg", class: 'rounded' %>
		        <% end %><br>
		        <%= link_to product.genre.name, admin_genres_path(product.genre_id) %><br>
		        <%= product.name %><br>
		        <%= (product.price * 1.1).floor %>円<br>
         
        <% end %>
	   
	 	<%= paginate @products %>
	 